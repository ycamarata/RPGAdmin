<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ajouter un Personnage</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-secondary text-light">

    <%- include('partials/navbar') %>
    <%- include('partials/adminBackButton') %>
    <div class="container mt-5">
        <h1 class="text-warning text-center">Ajouter un Personnage</h1>
        <% if (error_msg) { %>
            <div class="alert alert-danger"><%= error_msg %></div>
        <% } %>
        <form action="/admin/characters/add" method="POST" class="mt-4">
            <div class="mb-3">
                <label for="name" class="form-label">Nom</label>
                <input type="text" class="form-control" id="name" name="name" required>
            </div>
            <div class="mb-3">
                <label for="hp" class="form-label">Points de Vie (HP)</label>
                <input type="number" class="form-control" id="hp" name="hp" required>
            </div>
            <div class="mb-3">
                <label for="attack" class="form-label">Attaque</label>
                <input type="number" class="form-control" id="attack" name="attack" required>
            </div>
            <div class="mb-3">
                <label for="defense" class="form-label">Défense</label>
                <input type="number" class="form-control" id="defense" name="defense" required>
            </div>
            <button type="submit" class="btn btn-warning">Ajouter</button>
        </form>
        <div class="mt-4">
            <a href="/admin/characters" class="btn btn-info text-dark">Voir les Personnages Jouables</a>
        </div>
    </div>
   

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
