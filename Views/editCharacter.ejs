<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier le Personnage Jouable</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-secondary text-light">

    <!-- Navbar incluse -->
    <%- include('partials/navbar') %>
    <%- include('partials/adminBackButton') %>
    <div class="container mt-4">
        <h1 class="text-center text-warning">Modifier le Personnage Jouable</h1>

        <!-- Formulaire de modification -->
        <form action="/admin/characters/edit/<%= character.id %>" method="POST" class="d-flex flex-column">
            <div class="mb-4">
                <label for="name" class="form-label fs-4">Nom du Personnage</label>
                <input type="text" id="name" name="name" class="form-control fs-4" value="<%= character.name %>" required>
            </div>
         
            <div class="mb-4">
                <label for="hp" class="form-label fs-4">HP</label>
                <input type="number" id="hp" name="hp" class="form-control fs-4" value="<%= character.hp %>" required>
            </div>
            <div class="mb-4">
                <label for="attack" class="form-label fs-4">Attaque</label>
                <input type="number" id="attack" name="attack" class="form-control fs-4" value="<%= character.attack %>" required>
            </div>
            <div class="mb-4">
                <label for="defense" class="form-label fs-4">Défense</label>
                <input type="number" id="defense" name="defense" class="form-control fs-4" value="<%= character.defense %>" required>
            </div>
            <button type="submit" class="btn btn-dark btn-lg text-warning">Modifier le Personnage</button>
        </form>

        <!-- Bouton pour revenir à la gestion des personnages -->
        <div class="text-center mt-4">
            <a href="/admin/characters" class="btn btn-info text-dark">Retour à la Liste des Personnages</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
